<%= form_for(@admin_order_shipping_method) do |f| %>
  <% if @admin_order_shipping_method.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_order_shipping_method.errors.count, "error") %> prohibited this admin_order_shipping_method from being saved:</h2>

      <ul>
      <% @admin_order_shipping_method.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
