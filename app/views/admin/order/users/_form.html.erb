<%= form_for(:user, @admin_order_user) do |f| %>
  <% if @admin_order_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_order_user.errors.count, "error") %> prohibited this admin_order_user from being saved:</h2>

      <ul>
      <% @admin_order_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br />
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br />
    <%= f.text_field :last_name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :birth_date %><br />
    <%= f.date_select :birth_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
