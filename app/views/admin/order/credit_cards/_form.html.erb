<%= form_for(@admin_order_credit_card) do |f| %>
  <% if @admin_order_credit_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_order_credit_card.errors.count, "error") %> prohibited this admin_order_credit_card from being saved:</h2>

      <ul>
      <% @admin_order_credit_card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
