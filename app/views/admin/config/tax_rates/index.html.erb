<h1>Listing tax_rates</h1>

<table>
  <tr>
    <th>Percentage</th>
    <th>Product type</th>
    <th>State</th>
    <th>Start date</th>
    <th>End date</th>
    <th>Active</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tax_rates.each do |tax_rate| %>
  <tr>
    <td><%= tax_rate.percentage %></td>
    <td><%= tax_rate.product_type_id %></td>
    <td><%= tax_rate.state_id %></td>
    <td><%= tax_rate.start_date %></td>
    <td><%= tax_rate.end_date %></td>
    <td><%= tax_rate.active.to_s %></td>
    <td><%= link_to 'Show', admin_config_tax_rate_path(tax_rate) %></td>
    <td><%= link_to 'Edit', edit_admin_config_tax_rate_path(tax_rate) %></td>
    <td><%= link_to 'Destroy', admin_config_tax_rate_path(tax_rate), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tax rate', new_admin_config_tax_rate_path %>
